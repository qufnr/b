spring:
  application:
    name: Project B
  profiles:
    active: dev
  web:
    resources:
      add-mappings: false
server:
  port: 8172
# 전역 개인 설정
bserver:
  # 인증 설정
  verification:
    password:
      # 비밀번호 변경 인증 키 만료 시간 (분 단위)
      expiration: 5
  # JWT 설정
  jwt:
    # 만료 시간 (모두 분 단위)
    expiration:
      # 만료 시간 - 로그인 토큰
      sign: 1
      # 만료 시간 - 접근 토큰
      access: 1440
      # 만료 시간 - 리프레시 토큰
      refresh: 2880